export interface SlideProps {
  isActive: boolean;
}

export interface FlowType {
  name: string;
  icon: string;
  description: string;
  example: string;
}

export interface RoiState {
  docs: number;
  staff: number;
}
